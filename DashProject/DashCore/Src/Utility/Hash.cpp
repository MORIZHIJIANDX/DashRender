#include "PCH.h"
#include "Hash.h"

namespace Dash
{
/*
    // 基础类型
    int value = 42;
    size_t hash1 = Hash::HashObject(value);

    // 数组
    float array[100];
    size_t hash2 = Hash::HashState(array, 100);

    // 字符串
    const char* str = "Hello, World!";
    size_t hash3 = Hash::HashState(str, 1);  // 自动检测长度

    // 结构体
    struct MyStruct {
        int x, y, z;
        float w;
    };
    static_assert(Hash::is_trivially_hashable_v<MyStruct>);
    MyStruct obj = {1, 2, 3, 4.0f};
    size_t hash4 = Hash::HashObject(obj);

    // 组合多个值
    size_t combined = Hash::HashMultiple(value, str, obj);
*/
}